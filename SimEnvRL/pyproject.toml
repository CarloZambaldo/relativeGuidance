[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/SimEnvRL"]

[project]
name = "SimEnvRL"
version = "0.2.1"
dependencies = [
  "gymnasium",
  "pre-commit",
  "scipy",      # Scientific computing
  "numpy",      # Numerical computations
]